.category data-slug=category.slug data-article=article.slug
  = link_to category.title, category
  - if can?(:create, category) && can?(:destroy, category)
    - if article.categories.exists? id: category.id
      span.remove= link_to 'Remove', '#'
    - else
      span.add= link_to 'Add', '#'
