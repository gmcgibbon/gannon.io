<%= render(ApplicationPageComponent.new) do |page| %>
  <% page.with_title { yield(:title) } %>
  <% page.with_description { yield(:description) } %>
  <% page.with_body do %>
    <%= yield %>
  <% end %>
<% end %>
