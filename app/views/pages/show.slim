- @page_title = @page.title

h2= @page.title

- if can? :edit, @page
  span.edit= link_to 'Edit', edit_page_path(@page)
- if can? :destroy, @page
  span.delete= link_to 'Delete', page_path(@page), method: :delete, data: { confirm: t('gannon.confirm.delete') }

div.content = raw @page.content
